# Secret holding MySQL credentials for the backend API pods
---
apiVersion: v1
kind: Secret
metadata:
  name: backend-db-credentials
  namespace: backend
stringData:
  DB_USER: root
  DB_PASSWORD: password123
